<%= form_with model: [post, comment], local: false do |f| %>
  <%= f.text_area :comment, rows:'5', placeholder: "コメントをここに", class: "w-100 form-control", id: "comment_textarea" %>
  <%= f.submit "送信" %>
<% end %>